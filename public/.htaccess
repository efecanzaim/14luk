# 14'lük - React Router için URL Rewrites
# Bu dosya Natro hosting'de temiz URL'ler için gereklidir

<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /
  
  # www yönlendirmesi - www.14luk.com -> 14luk.com
  RewriteCond %{HTTP_HOST} ^www\.14luk\.com$ [NC]
  RewriteRule ^(.*)$ https://14luk.com/$1 [R=301,L]
  
  # HTTPS yönlendirmesi (SSL varsa)
  # RewriteCond %{HTTPS} off
  # RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]
  
  # Backend API isteklerini doğrudan geçir
  RewriteCond %{REQUEST_URI} ^/backend [NC]
  RewriteRule ^ - [L]
  
  # Mevcut dosya veya klasörse doğrudan göster
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  
  # Diğer tüm istekleri index.html'e yönlendir (React Router için)
  RewriteRule . /index.html [L]
</IfModule>

# Cache ayarları (Performans için)
<IfModule mod_expires.c>
  ExpiresActive On
  
  # Görseller - 1 yıl
  ExpiresByType image/jpg "access plus 1 year"
  ExpiresByType image/jpeg "access plus 1 year"
  ExpiresByType image/gif "access plus 1 year"
  ExpiresByType image/png "access plus 1 year"
  ExpiresByType image/webp "access plus 1 year"
  ExpiresByType image/svg+xml "access plus 1 year"
  
  # CSS ve JavaScript - 1 ay
  ExpiresByType text/css "access plus 1 month"
  ExpiresByType text/javascript "access plus 1 month"
  ExpiresByType application/javascript "access plus 1 month"
  
  # HTML - 10 dakika
  ExpiresByType text/html "access plus 10 minutes"
</IfModule>

# Gzip Sıkıştırma (Hız için)
<IfModule mod_deflate.c>
  AddOutputFilterByType DEFLATE text/html
  AddOutputFilterByType DEFLATE text/css
  AddOutputFilterByType DEFLATE text/javascript
  AddOutputFilterByType DEFLATE application/javascript
  AddOutputFilterByType DEFLATE application/json
  AddOutputFilterByType DEFLATE application/xml
</IfModule>

# Güvenlik Headers
<IfModule mod_headers.c>
  # XSS Koruması
  Header set X-XSS-Protection "1; mode=block"
  
  # Clickjacking Koruması
  Header set X-Frame-Options "SAMEORIGIN"
  
  # MIME Sniffing Koruması
  Header set X-Content-Type-Options "nosniff"
  
  # Referrer Policy
  Header set Referrer-Policy "strict-origin-when-cross-origin"
</IfModule>

# Dosya listesini gizle
Options -Indexes

# .env dosyasını web'den erişime kapat
<Files ".env">
  Order allow,deny
  Deny from all
</Files>

